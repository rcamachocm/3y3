<!--
  Wazuh - Manager - Default configuration for ubuntu 20.04
  More info at: https://documentation.wazuh.com
-->

<ossec_config>
  <global>
    <jsonout_output>yes</jsonout_output>
    <alerts_log>yes</alerts_log>
    <logall>no</logall>
    <logall_json>no</logall_json>

    <!-- E-mail (desabilitado, mas configurado) -->
    <email_notification>no</email_notification>
    <smtp_server>smtp.example.wazuh.com</smtp_server>
    <email_from>wazuh@example.wazuh.com</email_from>
    <email_to>recipient@example.wazuh.com</email_to>
    <email_maxperhour>12</email_maxperhour>
    <email_log_source>alerts.log</email_log_source>

    <!-- Tempo até considerar agente desconectado -->
    <agents_disconnection_time>10m</agents_disconnection_time>
    <agents_disconnection_alert_time>0</agents_disconnection_alert_time>
  </global>

  <alerts>
    <log_alert_level>3</log_alert_level>
    <email_alert_level>12</email_alert_level>
  </alerts>

  <!-- Monitoramento dos logs do Manager -->
  <logging>
    <log_format>plain</log_format>
  </logging>

  <!-- Ligações com agentes -->
  <remote>
    <connection>secure</connection>
    <port>1514</port>
    <protocol>tcp</protocol>
  </remote>

  <!-- Ativar gerenciamento de arquivos (FIM) -->
  <rootcheck>
    <disabled>no</disabled>
  </rootcheck>

  <syscheck>
    <disabled>no</disabled>
    <frequency>3600</frequency>
    <scan_on_start>yes</scan_on_start>
    <directories check_all="yes">/etc,/usr/bin,/usr/sbin</directories>
  </syscheck>

  <!-- Regras -->
  <ruleset>
    <rule_dir>/var/ossec/rules</rule_dir>
    <decoder_dir>/var/ossec/decoders</decoder_dir>
    <rule_dir>ruleset/rules</rule_dir>
    <decoder_dir>ruleset/decoders</decoder_dir>
    <list_dir>ruleset/lists</list_dir>
    <rule_exe>ruleset/rules</rule_exe>

    <!-- Regras personalizadas -->
    <rule_dir local="yes">rules/local_rules.xml</rule_dir>
  </ruleset>

  <!-- Active Response (respostas automáticas) -->
  <command>
    <name>firewall-drop</name>
    <executable>firewall-drop</executable>
    <timeout_allowed>yes</timeout_allowed>
  </command>

  <active-response>
    <command>firewall-drop</command>
    <location>local</location>
    <level>7</level>
  </active-response>

  <!-- API -->
  <api>
    <enabled>yes</enabled>
    <listen_addr>0.0.0.0</listen_addr>
    <port>55000</port>
  </api>

  <!-- Cluster (se não usa cluster, deixar desativado) -->
  <cluster>
    <disabled>yes</disabled>
  </cluster>

</ossec_config>
